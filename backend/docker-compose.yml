services:
  postgres:
    image: postgres:16
    container_name: gym
    environment:
      POSTGRES_DB: gym_dev
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5433:5432"
    volumes:
      - gymdata:/var/lib/postgresql/data

volumes:
  gymdata: