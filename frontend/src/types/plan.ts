export type Plan = {
    id: number
    name: string
    description: string
    price: number
    maxStudents: number
    maxPrograms: number
    benefits: string[]
    active: boolean
    createdAt: string
    updatedAt: string
}
